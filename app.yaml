application: w-avatarproxy
version: 1
runtime: python
api_version: 1

handlers:
- url: /(.*\.(gif|png|jpg))
  static_files: \1
  upload: img/(.*\.(gif|png|jpg))
- url: /(i|t|h)/.*
  script: handler.py
- url: .*
  script: main.py
